/**
 * @peasant-labs/transcript-browser
 *
 * React viewer for AI agent session transcripts.
 *
 * SCAFFOLD ONLY — this package intentionally exports nothing yet. The viewer
 * components (currently living in peasant/web and village/frontend) will be
 * lifted here in a later task. Re-exporting the shared package version below
 * keeps this module from being an empty (illegal) ES module and gives consumers
 * a single stable entry point to import from once components arrive.
 */

/** Package version marker. Replaced by real component exports later. */
export const VERSION = "0.0.0";
