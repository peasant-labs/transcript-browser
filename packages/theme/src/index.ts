/**
 * @peasant-labs/theme
 *
 * Design tokens for the transcript browser. Import the raw CSS variables via
 * `@peasant-labs/theme/tokens.css`, and the typed token-name map / Tailwind
 * preset placeholder from this entry point.
 */

export { themeTokens, tokensStylesheetPath } from "./preset.js";
export type { ThemeTokenName } from "./preset.js";
